اريد اعدادات الطباعة ان تكون احترافية وافضل من الانظمة العالمية ولا اريد ان تكون بيانات ثابتة بل المستخدم يتحكم في كل شيء ويتيح له تحديد اكثر من نموذج لاي مستند كما يحدد الافتراضي ويحفظ في قاعدة البيانات لانه نظام يعمل به فريق عمل اذا اردت اي ملف اخر سوف ازودك به لا تعمل على تكهنات او افتراضيات اعمل على الملفات التي لدي باحترافية


المشاكل المكتشفة:

handleSave يحفظ في localStorage فقط — يجب حفظ في DB عبر settingsApi
قالب واحد لكل المستندات (erp_print_tpl_FV_80mm فقط) — يجب قالب منفصل لكل (docTypeCode, paperSize)
rawPrintSettings مُجلَب لكن لا يُستخدم لتحميل القوالب
تبديل selectedDoc لا يُحمِّل قالبه من DB/localStorage
tpl state واحدة مشتركة بين كل المستندات
useCurrentCompany — يجب التحقق من وجوده في الملفات
